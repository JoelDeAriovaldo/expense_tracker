import 'package:flutter/material.dart';

class LoginVie extends StatelessWidget {
  const LoginVie({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
